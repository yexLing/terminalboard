"""terminalboard — a pure-terminal TensorBoard viewer.

Reads TensorBoard event logs and renders them live inside any terminal (local or
SSH) as Unicode/braille text — scalars as curves, text summaries, histograms as
heatmaps or distribution bands, precision-recall curves, and an HParams table.
No browser, no X11, no port forwarding.
"""

__version__ = "0.5.2"
